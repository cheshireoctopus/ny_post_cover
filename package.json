{
  "name": "ny_post_cover",
  "version": "1.0.0",
  "description": "Get the Post's cover texted to your phone every morning.",
  "main": "app/build.js",
  "author": "cheshireoctopus",
  "scripts": {
    "build": "babel --presets env app/index.js --out-file app/build.js",
    "watch": "babel --presets env app/index.js -w --out-file app/build.js"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "cheerio": "^0.22.0",
    "dotenv": "^2.0.0",
    "express": "^4.14.0",
    "node-schedule": "^1.2.0",
    "request": "^2.76.0",
    "twilio": "^2.11.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "nodemon": "^1.12.1"
  }
}
